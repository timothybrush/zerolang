fn fibWordLen u32 n u32
  mut a u32 1
  mut b u32 1
  mut i u32 0
  while < i n
    let next + a b
    set a b
    set b next
    set i + i 1
  ret a

pub fn main Void world World !
  if == (fibWordLen 6) 13
    check world.out.write "fibonacci word ok\n"
