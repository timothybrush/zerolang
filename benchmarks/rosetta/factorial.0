fn factorial u32 n u32
  mut value u32 1
  mut i u32 2
  while <= i n
    set value * value i
    set i + i 1
  ret value

pub fn main Void world World !
  if == (factorial 6) 720
    check world.out.write "factorial ok\n"
