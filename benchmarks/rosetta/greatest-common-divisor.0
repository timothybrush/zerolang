fn gcd u32 a0 u32 b0 u32
  mut a a0
  mut b b0
  while != b 0
    let next % a b
    set a b
    set b next
  ret a

pub fn main Void world World !
  if == (gcd 84 30) 6
    check world.out.write "gcd ok\n"
