pub fn main Void world World !
  let values [4]u32 [1, 2, 3, 4]
  mut i usize 0
  mut sum u32 0
  mut product u32 1
  while < i 4
    set sum + sum values[i]
    set product * product values[i]
    set i + i 1
  if && (== sum 10) (== product 24)
    check world.out.write "sum product ok\n"
