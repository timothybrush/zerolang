fn squareSeries u32 n u32
  mut total u32 0
  mut i u32 1
  while <= i n
    set total + total (* i i)
    set i + i 1
  ret total

pub fn main Void world World !
  if == (squareSeries 5) 55
    check world.out.write "series ok\n"
