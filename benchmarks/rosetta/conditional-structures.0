fn sign i32 value i32
  if < value 0
    ret - 0 1
  else
    if > value 0
      ret 1
  ret 0

pub fn main Void world World !
  if && (== (sign (- 0 4)) (- 0 1)) (== (sign 7) 1)
    check world.out.write "conditional structures ok\n"
