fn replaceFirst u32 text Span<u8> from u8
  mut i usize 0
  while < i (std.mem.len text)
    if == text[i] from
      ret i as u32
    set i + i 1
  ret 999

pub fn main Void world World !
  if == (replaceFirst (std.mem.span "abca") 99) 2
    check world.out.write "markov ok\n"
