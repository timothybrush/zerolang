fn even Bool n u32
  if == n 0
    ret true
  ret odd (- n 1)

fn odd Bool n u32
  if == n 0
    ret false
  ret even (- n 1)

pub fn main Void world World !
  if && (even 10) (odd 9)
    check world.out.write "mutual recursion ok\n"
