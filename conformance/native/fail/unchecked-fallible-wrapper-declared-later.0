shape Counter {
    value: i32,

    fun risky(self: ref<Self>) -> Void raises { Bad } {
        raise Bad
    }
}

pub fun main() -> Void {
    let counter: Counter = Counter { value: 0 }
    wrapper(&counter)
}

fun wrapper(counter: ref<Counter>) -> Void raises {
    check counter.risky()
}
