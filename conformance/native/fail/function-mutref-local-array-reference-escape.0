shape Holder {
    values: [1]ref<i32>,
}

fun storeLocal(holder: mutref<Holder>) -> Void {
    let local = 1
    holder.values = [&local]
}

pub fun main() -> Void {
    let fallback = 0
    let mut holder = Holder { values: [&fallback] }
    storeLocal(&mut holder)
}
