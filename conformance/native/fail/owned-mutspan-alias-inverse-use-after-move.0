type File {
    fd: i32,
}

fn close(file: owned<File>) -> Void {
}

pub fn main() -> Void {
    var files: [1]owned<File> = [File { fd: 1 }]
    let view: MutSpan<owned<File>> = files
    close(files[0])
    close(view[0])
}
