shape Holder {
    value: ref<i32>,
}

fun storeRecursive(holder: mutref<Holder>, next: ref<i32>) -> Void {
    storeRecursive(holder, next)
}

pub fun main() -> Void {
    let old = 1
    let next = 2
    let mut holder = Holder { value: &old }
    storeRecursive(&mut holder, &next)
}
