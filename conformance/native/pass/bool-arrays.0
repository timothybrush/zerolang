pub fn main Void world World !
  mut flags [5]Bool [false;5]
  set flags[1] true
  set flags[3] true
  if && (&& (== flags[0] false) flags[1]) (&& flags[3] (== flags[4] false))
    check world.out.write "bool arrays ok\n"
