shape Holder {
    value: ref<i32>,
}

fun store(holder: mutref<Holder>, next: ref<i32>) -> Void {
    holder.value = next
}

pub fun main() -> Void {
    let old = 1
    let next = 2
    let mut holder = Holder { value: &old }
    store(&mut holder, &next)
}
